#!/usr/bin/env bash
set -euo pipefail
REPO_ROOT="$(git rev-parse --show-toplevel)"
cd "$REPO_ROOT"
xcodebuild test \
  -scheme {{SCHEME}} \
  -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' \
  | xcpretty || exit 1
